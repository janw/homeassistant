
  - alias: Toggle working lights in kitchen if flic is pressed
    trigger:
      platform: event
      event_type: flic_click
      event_data:
        button_name: flic_80e4da700a82
        click_type: single
    action:
      service: homeassistant.toggle
      entity_id: light.kuche_arbeitslicht

  - alias: Toggle stereo in kitchen if flic is pressed
    trigger:
      platform: event
      event_type: flic_click
      event_data:
        button_name: flic_80e4da700a82
        click_type: hold
    action:
      service: switch.toggle
      entity_id: switch.kitchen_stereo

