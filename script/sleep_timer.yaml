
sleep_timer:
  alias: 'Sleep'
  sequence:
    - delay:
        minutes: '{{ states.input_select.sleep_minutes | int }}'
    - service: switch.turn_off
      data:
        entity_id:
          - group.all_switches
    - service: light.turn_off
      data:
        entity_id:
          - group.all_lights
